# Codex starting assignment

Repository: `erpzz/Nightwatcher`. Proposed import branch: `nightwatcher/bootstrap-nw00`.

## Import gate

This package was prepared locally because the chat GitHub connection returned `403 Resource not accessible by integration` for branch creation. The files are not evidence of a remote push. Use your own authorized Codex repository environment; do not assume this chat has launched one.

Read and inspect the supplied code before execution. Record the actual current HEAD and tree. The previously observed base was `ed98f478d270946ed2da431fdcb23ee701a1a5d7`. If it has moved, inspect the intervening changes and reconcile instead of overwriting or force-pushing. Do not make a new README-only repository or replace Git history to reproduce a test fixture.

Verify the import patch and proposed manifest against their independently retained digests. `audit/PROPOSED_MANIFEST.json` excludes itself and lists its exact scope. A matching snapshot is not release authorization.

## First bounded assignment: finish NW-00

Permitted paths for this assignment: `tools/`, `tests/`, `audit/`. Changes to root instructions or `docs/handoff/` require a separate explanation and review. Preserve the archived handoffs byte-for-byte.

1. Recheck repository refs, original objects, branch rules and available audit evidence using actual authorized reads. Reconcile the recorded tree-size/text-length metadata discrepancy without assuming malicious intent.
2. Run the 28 bootstrap tests from the repository root and inspect the checker independently. Verify process return codes for an unchanged source tree and for a controlled change in a disposable copy.
3. Propose real protected-branch/release controls and a separately administered witness. Do not mark them enabled without verifying them. CODEOWNERS by itself is not enforcement, and matching the local manifest never sets `release_authorized` to true.
4. Return a small PR, exact before/after revisions, changed-file digest, actual test output, unknowns and approval state. Do not merge your own security-sensitive changes.

NW-00 remains partial until independent review and actual release enforcement exist. Do not call the bootstrap a clean historical audit.

## Then proceed to NW-01 and NW-02

NW-01: reverify the source register against current primary documents, including corrections and operator/researcher disagreements. Neither original handoff's claim of prior verification is sufficient evidence of current activity.

NW-02: implement the durable authenticated evidence foundation and one real authorized observation-to-export path. Keep public intelligence, historical/lab records and operational telemetry separate. No dashboard work until that path exists. Record unavailable network/source access accurately rather than generating dummy live events.

Vercel remains the intended private application target, but no deployment or billable service is authorized by this initial assignment. Continue sequentially when no actual subagent interface exists. Report exact completed work, not another broad plan.
