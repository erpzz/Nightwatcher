# R05 - Public fieldwork and persistent-report watch

Owner-authorized NightWatcher work; review date September 15, 2026 (America/New_York).
Base observed before this documentation change: `487af41a6d0dad9879c741829166d0df5a1c5db8`.
This is a new research note, not a retry of the blocked full runtime import or PR operation.

## Findings

A complete displayed archive page contains a June 16 request at 12:30:20, a follow-up inquiry
at 12:47:45, and a June 26 deletion. The 1,045-second difference is arithmetic on displayed
archive timestamps, not verified event latency. The follow-up asks for status; it does not
confirm successful delivery of the requested answer. Names do not authenticate distinct agents.

Source: https://collusion.wiki/explorer/page/dse~CashierR5UrgentJan12
All three displayed history records were inspected. Other linked pages were not presumed read.

The current public site index displays 30 sites and 7,203 under its agent-edit heading. Its rows
mix revisions, links, pastes, packages and documents; this is not a count of live swarms.
Source: https://collusion.wiki/explorer/sites/

An authorized connector read of all NightWatcher pull requests returned `[]`. This is a snapshot,
not a continuously running PR watch. Several requested archive/paste pages returned non-retryable
web-tool errors; no alternate route or covert access was attempted.

## Persistent intelligence

An hourly ChatGPT task is now enabled for bounded primary-report research and material corrections.
It is NOT private-runtime telemetry, an invisible presence in a chatroom, an actual GitHub webhook,
or a process updating the local database. No routine no-change alerts are requested.

The intended low-latency path for the owned repository is a signed, scoped webhook receiver with
durable event storage and non-executing PR review. That receiver is not configured yet. Public
report delay includes publication, collection, processing and review; hourly checks are not real-time.

## Local artifacts, not imported runtime

The updated SQLite casebook contains 5 cases, 14 sources, 28 observations, 5 relationships, 8 leads,
2 historical indicators and 2 repository collection records. The original database is preserved.
A separate watch database contains 5 structured baseline captures and explicit blocked/degraded
collection states. These are rendered/manual or connector observations, not verified origin bytes.

New local tools provide bounded enrolled-source observation and an optional real-model laboratory
runner. No model was invoked: the runner's receipt says NOT_EXECUTED. It requires an authorized
model, private local key and explicit cost approval; it has no simulated-model fallback. No
subagents were launched, no original-site messages were sent, and no private room was entered.

The later source code remains in the downloadable source package, not in this documentation
commit. No merge, production release or full source synchronization is implied. Two local SQL
import placeholder errors were corrected before the final casebook was committed/exported;
the original database was unchanged. Validation was limited to syntax, real imports/exports,
DB integrity/reference checks and a direct collector attempt that reported network failure.
The large historical regression suite was not rerun.

## Next discriminating evidence

1. An owner-authorized receiving service or repository webhook stream, with recorded latency.
2. Actual model responses in the bounded ticket task, not a deterministic stand-in.
3. Operator-supported task scope and actor continuity before promoting a coordination lead.

Source context: https://metr.org/blog/2026-08-26-openai-hugging-face-incident-investigation/
Provider context: https://openai.com/hugging-face-incident-and-misalignment/
Webhook design: https://docs.github.com/en/webhooks/using-webhooks/best-practices-for-using-webhooks

No active rogue swarm, compromise of NightWatcher, covert membership, or global visibility is established.
